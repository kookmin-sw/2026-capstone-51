# 로컬 override 예시.
# 이 파일을 .env.local 로 복사하면 .env 보다 우선 적용됨 (.env.local 은 git ignored).
#
# 백엔드를 본인 머신에 직접 띄워서 테스트할 때:
# VITE_API_URL=http://localhost:8080/api
#
# 실서버 (default 는 .env 의 https://3.238.29.250/api).
# 자체서명 인증서라 브라우저 첫 방문 시 "고급 → 안전하지 않은 페이지로 진행" 한 번 눌러야 함.
